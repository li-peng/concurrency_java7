2.2 synchronized关键字 demo1
2.3 synchronized关键字 :同一个对象，两个锁控制两个属性的访问。-- demo2
2.4 在同步代码块中使用条件：wait,notify等关键字，必须在同步代码块中使用，否则会抛出异常。例如：缓存区是满的，生产者就不能再放入数据。
    当一个线程调用wait方法时，JVM将这个线程休眠，并且释放控制这个同步代码块的对象锁，同时允许其他线程执行这个对象控制的其他同步代码块。为了唤醒这个线程，必须在这个对象控制的某个
    某个代码块中调用notify或者notifyAll方法。